#include <stdio.h>

/**
 * main - displays all digit numbers starting from 0 to 10
 * Return: always 0, success
 */

int main(void)
{
	int n = 0;

	while (n < 0)
	{
		printf("%i", n);
		n++;
	}
	printf("\n");

	return (0);
}
